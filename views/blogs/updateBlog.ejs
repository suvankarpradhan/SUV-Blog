<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/header-link') %>
  </head>
  <body>
    <%- include('../partials/navbar') %>
    <div class="container my-5">
      <h2 class="text-center">Update this Blog</h2>
      <div class="row justify-content-center">
        <form
          action="/blog/update/<%=blogData._id%>"
          method="POST"
          class="col-8"
        >
          <div class="form-group my-4">
            <label for="title" class="form-label">Blog Title</label>
            <input
              type="text"
              class="form-control"
              id="blogTitle"
              name="blogTitle"
              value="<%= blogData.blogTitle %>"
              placeholder="Give a suitable blog name.."
            />
          </div>
          <div class="form-group mb-4">
            <label for="blogBody" class="form-label">Write Something</label>
            <textarea
              class="form-control"
              id="blogBody"
              name="blogBody"
              rows="6"
            >
        <%=blogData.blogBody%> </textarea
            >
          </div>
          <div class="d-flex justify-content-md-center">
            <button type="submit" class="btn btn-primary mx-2">Update</button>
            <a type="button" class="btn btn-outline-secondary mx-2" href="/blog"
              >Back</a
            >
          </div>
        </form>
      </div>
    </div>
    <%- include('../partials/footer-link') %>
  </body>
  <%- include('../partials/footer') %>
</html>
